// Example program
#include <iostream>
#include <string>

class Parser
{
  public:
    Parser(const std::string pparse) : parse_str{setTextToParse(pparse)} { }
    const std::string parse_str;
    
  private:
    std::string setTextToParse(std::string pparse);
    void sayHello();
};

std::string Parser::setTextToParse(std::string pparse) {
    std::string parse = pparse;
    // throw 20;
      try {
   throw 30;   
  } catch (int &e) {
      std::cout << e << std::endl;
  }
    return parse;
}

void Parser::sayHello() {
    std::cout << "Hi!" << std::endl;
}

int main()
{
  std::string name;
  std::cout << "What is your name? ";
  getline (std::cin, name);
  Parser parser{name};
  try {
   throw 20;   
  } catch (int &e) {
      std::cout << e << std::endl;
  }
  std::cout << "Hello, " << parser.parse_str << "!\n";
//   parser.parse_str = "qwerty";
//   std::cout << "Hello, " << parser.parse_str << "!\n";
}